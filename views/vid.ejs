<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, shrink-to-fit=no">
    <title>Coronavirus</title>
    <link rel="stylesheet" href="/assets/bootstrap/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Hind">
    <link rel="stylesheet" href="/assets/css/Footer-Basic.css">
    <link rel="stylesheet" href="/assets/css/styles.css">
	<script>
		function contactar(){
			alert("Función siendo programada");
		}
	</script>
	
</head>

<body>
    <nav class="navbar navbar-light navbar-expand-md">
        <div class="container-fluid"><a class="navbar-brand" href="/" style="font-family: Hind, sans-serif;font-style: normal;">cobot19</a><button data-toggle="collapse" class="navbar-toggler" data-target="#navcol-1"><span class="sr-only">Toggle navigation</span><span class="navbar-toggler-icon"></span></button>
            <div
                class="collapse navbar-collapse" id="navcol-1">
                <ul class="nav navbar-nav">
                    <li class="nav-item" role="presentation" id="desktopBrand"><a class="nav-link active" href="/">cobot19</a></li>
                    <li class="nav-item" role="presentation"><a class="nav-link" href="/verArchivos.html">archivos</a></li>
                    <li class="nav-item" role="presentation"><a class="nav-link" href="/TEMP.html">comparaciones</a></li>
                    <li class="nav-item" role="presentation"><a class="nav-link" href="/TEMP.html">edición</a></li>
                </ul>
        </div>
        </div>
    </nav>
<!--     <p id="rightZIndex">Vos:</p> -->
    <!-- <p id="leftZIndex">Mundo:</p> -->
	<div id="vidContainer" style="width:100%;">
		<video controls="true" type='video/mp4' src="<%= "/uploads"+ videoURL %>" preload="metadata" playsinline style="height:30vh; width:30vw; position:relative; margin-left:40vw; transform: translate(-50%, 0);">
    	</video>
		<p style="position:relative; margin-left:40vw; transform: translate(-50%, 0); text-align:center;"> Autor: <%= author %> </p>
		<button class="btn btn-primary" type="button" style="background-color: rgb(0,0,0);position:relative; margin-left:40vw; transform: translate(-50%, 0)" id="btn-upload" onclick="contactar()">Contactar al Autor</button>
    <br />
	</div>
    <div  id="videosContainerHorizontal" >

	</div>

    <div style="height:13vh;"> </div>
    <div class="footer-basic">
        <footer><a href="/nosotros.html">nosotros</a><div></div><a href="#">aviso de privacidad</a></footer>
    </div>

    <script src="/assets/js/jquery.min.js"></script>
    <script src="/assets/bootstrap/js/bootstrap.min.js"></script>
	<script>
		function shuffle(array) {
			var currentIndex = array.length, temporaryValue, randomIndex;
			while (0 !== currentIndex) {

				randomIndex = Math.floor(Math.random() * currentIndex);
				currentIndex -= 1;

				temporaryValue = array[currentIndex];
				array[currentIndex] = array[randomIndex];
				array[randomIndex] = temporaryValue;
			}

			return array;
		}

		var toInsert = "";
		var counter = 0;
		var displayCounter = 0;
		var DISPLAYCOUNTERMAX = 8;
		
		$(document).ready(() => {
			var res = ""; //'?page='+getUrlVars()["page"] ? window.location.href.search('page') != -1 : """";
			 $.getJSON('/archivos' + res, function(archivos){
				 archivos = shuffle(archivos)
				archivos.forEach((archivo) =>{
					if(archivo.videoURL == 'SIN URL' || displayCounter++ > DISPLAYCOUNTERMAX)
						return;
					let toInsert = '<a href="/vid'+archivo.videoURL+'" id="videoLink'+counter+'"><img src="/thumbs'+archivo.videoURL.replace("mp4", "png")+'" loading="lazy" id="videoImage'+(counter++)+'" class="videoImageHorizontal"/></a>';
					$('#videosContainerHorizontal').append(toInsert);
				});
			});
			
		});
	</script>
</body>

</html>